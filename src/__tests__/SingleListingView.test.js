import React from "react";
import { render, cleanup, act } from "@testing-library/react";

it("lets the user create a reservation for this listing with a startDate and endDate", () => {});
it("displays confirmation after creating a reservation", () => {});
it("Renders a card with more details about the listing", () => {});
it("Shows a rating for the listing", () => {});
it("Displays the distance from currentLocation to this listing", () => {});
it("Allows the user to heart or save this listing to a group or favorites list", () => {});
it("shows the city state zip of current listing", () => {});
it("shows the main image of current listing", () => {});
it("shows a price per night for current listing", () => {});
