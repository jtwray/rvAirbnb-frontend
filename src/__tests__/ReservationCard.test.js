import React from 'react'
import{render, cleanup, act} from '@testing-library/react'


it("Renders this trips reserved dates",()=>{})
it("Renders the main image for this trip",()=>{})
it("Renders a time countdown for until the trip begins from todays date",()=>{})
it("Renders a distance countdown from currentlocation ",()=>{})

it("Renders a rating if one exists ",()=>{})
it("Renders the city, state of the reservation listing ",()=>{})

it("Routes the user to a new screen with more information when clicked",=>{}

