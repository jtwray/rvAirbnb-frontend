import React from 'react'
import{render, cleanup, act} from '@testing-library/react'



it("Renders a list of upcoming reservation cards ",()=>{})
it("render pagination buttons if more reservations exist than will fit on the screen ",()=>{})
